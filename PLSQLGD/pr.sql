CREATE OR REPLACE SHOW_STRENGTH
(
   P_DNO DEPT.DEPTNO%TYPE
)
IS
X BOOLEAN;
CNT NUMBER;
BEGIN
  X:=IF(VALID_DEPT_NO(20)) THEN
  DBMS_OUTPUT.PUT_LINE('DEPT FOUND');
ELSE
  DBMS_OUTPUT.PUT_LINE('DEPT NOT FOUND');
END IF;



END;
/